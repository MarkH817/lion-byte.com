---
import type { Person, WebSite, Graph, BlogPosting } from 'schema-dts'

interface Props {
  schema: Person | WebSite | BlogPosting | Graph
}
const { schema } = Astro.props
---

<script
  is:inline
  type="application/ld+json"
  set:html={JSON.stringify(schema)}
/>
